import React, { useState, useRef, FormEvent } from "react"
import useInput from "../../hooks/useInput"
import Input from "../common/Input"
import styled from "styled-components"

export default function HostRegisterComponents() {}
